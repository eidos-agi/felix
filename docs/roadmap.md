# Felix Roadmap

## Phase 1: Felix Exists

- Create installable `felix` CLI.
- Track the agent registry.
- Define the standard shape for every agent.
- Maintain a repo-native wiki and task list for Felix itself. Default adapter: Scridos.
- Live under `~/repos-eidos-agi/felix`.
- Carry open-source health files and self-documentation. Default adapter: FOSS Forge.

## Phase 2: Planned Agents

- Create Knox as the secrets/access agent.
- Create Capcom as the mission-control communicator for important outreach.
- Create Dewey as the AI librarian for local indexed context retrieval.
- Ensure both repos have CLI, wiki, project, milestones, tasks, tests, install, commit, and push.

## Phase 3: Maintenance

- Add `felix audit` to verify agent health.
- Add `felix scaffold` to create a new agent repo from the standard pattern.
- Add agent adapters so Felix can inspect and repair agents through a common capability interface.
- Add repair playbooks for stale wikis, broken installs, missing tests, missing remotes, and unpushed work.
